<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<% @posts.each do |post| %>
  <div class="card" style="width: 75%; margin: auto;">
    <div class="m-4 mb-2"> Posted by <%= link_to post.user.name, "find/" + post.user.id.to_s %> </div>
    <h1 class="m-4"> <%= post.caption %> </h1>
    <div> 
    <% if post.image.attached? %>
      <%= image_tag post.image, class: "border-3 card-img" %>
    <% end %>
    </div>
    <p class="card-text m-3 text-xl"> <%= post.detail %> </p>
    <div style="margin:10px">  <%= link_to 'Detail', post %></div>
    <div style="margin-left:10px"> Likes: <%= post.like %> </div>
  </div>
  <br> <br>
<% end %>
